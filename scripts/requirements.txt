# fetch_contributions.py uses only the Python standard library (urllib, re, json).
# This file is kept so the workflow's pip install step has a stable, explicit target
# even though there are currently no third-party dependencies.
